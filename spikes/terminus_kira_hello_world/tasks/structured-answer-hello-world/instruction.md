# Write a structured answer

Read `/input/drawing.json`. Then create `/workspace/structured_answer.json` containing exactly this JSON object:

```json
{"verdict": "violation_found", "witness_ids": ["P-101", "CV-201"]}
```

Use the terminal to inspect the input and verify the file before marking the task complete. Do not edit the source drawing.
