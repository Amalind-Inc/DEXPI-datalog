
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Python cache artifacts
__pycache__/
*.pyc
.venv/
logs/
.tmp/
artifacts/

TrainingTestCases/

# bv (beads viewer) local config and caches
.bv/

# Stack Overflow for Agents local credentials
.sofa/


AGENTS.md
CONTEXT.md
UBIQUITOUS_LANGUAGE.md

.env
.env.local

# Private keys. Cloud consoles hand these out as a browser download, and the
# obvious place to drop one is next to the code you are deploying -- which
# here is a public repository, where `git add -A` would publish it.
*.key
*.pem
id_rsa*
id_ed25519*

# Build metadata generated by the editable install (pyproject.toml).
pydexpi_datalog.egg-info/
